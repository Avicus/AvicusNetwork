# Unnused - for DB config initializtion
mysql:
  host: localhost
  username: username
  password: password
  database: hook

redis:
  host: localhost
  database: 0
  auth:
    enabled: false
    password: password